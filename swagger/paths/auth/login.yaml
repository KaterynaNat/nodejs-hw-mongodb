post:
  tags:
    - Auth
  summary: Логін користувача
  operationId: login
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../components/Auth.yaml#/schemas/LoginInput
  responses:
    '200':
      description: Успішна авторизація
    '401':
      description: Невірна пошта або пароль
